"""Legacy non-image compatibility stubs.

EVAVO Local Image Generator owns production image generation through native
ComfyUI. Historical audio, video, 3D, particle and PBR-texture imports remain
available only to fail explicitly with ``UnsupportedGenerationError``; they do
not provide multimodal generation in this repository.
"""
