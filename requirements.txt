# EVAVO Local Image Generator - Production Dependencies
# Supported runtime: Python 3.10+
#
# Core operational utilities remain standard-library friendly where possible.
# The unified HTTP gateway adds the FastAPI stack below.

# Agent/MCP integration (current stable v2 line + HTTP/CLI transport runtime)
mcp[cli]>=2,<3

# Unified HTTP gateway
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.7,<3
websockets>=13,<17

# Core dependencies
requests>=2.28.0
python-dotenv>=0.21.0

# Image/numerical functionality
Pillow>=9.0.0
numpy>=1.21.0

# Async integrations used by gateway and repository adapters
aiohttp>=3.8.0

# Development / validation
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.950
