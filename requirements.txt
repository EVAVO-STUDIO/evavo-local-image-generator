# EVAVO Local Image Generator - Production Dependencies
# Supported runtime: Python 3.10+
#
# The operational control plane (evavo_operations.py, evavo-wrapper.py,
# mock-comfyui-server.py, generate-batch.py, monitor-evavo.py and
# task-tracker.py) intentionally uses only the Python standard library.
# The packages below support the wider generator/tooling repository.

# Agent/MCP integration (current stable v2 line + HTTP/CLI transport runtime)
mcp[cli]>=2,<3

# Core dependencies
requests>=2.28.0
python-dotenv>=0.21.0

# Image/numerical functionality
Pillow>=9.0.0
numpy>=1.21.0

# Async integrations used elsewhere in the repository
aiohttp>=3.8.0

# Development / validation
pytest>=7.0.0
pytest-cov>=4.0.0
black>=22.0.0
flake8>=4.0.0
mypy>=0.950
